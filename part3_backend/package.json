{
  "name": "helloworld",
  "version": "1.0.0",
  "description": "'part 3 first exercise'",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../part2_frontend && npm run build && @powershell Copy-Item dist -Recurse ../part3_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
